# <% tp.date.now("YYYY-MM-DD") %>

## 💡 문제 이해
> 문제를 나만의 언어로 요약하고, 데이터의 핵심 요구사항을 파악합니다.

- **핵심 조건**: 
    - (예: SHIPMENT_ID가 NULL인 경우 제외)

---

## 🔍 테이블 분석 및 접근 전략 (Query Plan)
> 쿼리를 작성하기 전, 테이블 간의 관계(Join)와 데이터 필터링 전략을 구상합니다.

1.  **사용 테이블 & 조인 키**:
    - `FIRST_HALF` (f) JOIN `ICECREAM_INFO` (i) ON `f.FLAVOR = i.FLAVOR`
2.  **함수 및 로직**:
    - `GROUP BY`: 맛 별로 묶기 위해 사용
    - `SUM()`: 총 주문량 계산
    - `LIMIT 3`: 상위 3개 추출

---

## 💻 쿼리 및 문제 설명
> 백준허브가 자동 생성한 **문제 폴더**로 이동합니다.

- [📂 문제 풀이 폴더 열기 (소스코드 & README)](../../프로그래머스/)

---

## 📝 실행 계획 & 최적화
> (중요) 단순 정답을 넘어, 성능을 고려한 흔적을 남기세요.

- **예상 실행 계획 (Explain)**:
    - (예: `FLAVOR` 컬럼에 인덱스가 있어서 Index Scan을 탐)
    - (예: `Using temporary`가 발생하여 정렬 비용이 높을 것으로 예상됨)
- **개선 아이디어**:
    - (예: `WHERE` 절로 데이터 범위를 먼저 줄이고 조인하는 것이 더 효율적일 듯)

---

## ❌ 오답 노트 & 회고
> 실수한 문법이나 새로 알게 된 함수를 정리합니다.

- **틀린 이유**: (예: `HAVING` 절은 `GROUP BY` 뒤에 와야 하는데 순서를 틀림)
- **배운 점**: `DENSE_RANK()`와 `RANK()`의 차이점 (동점자 처리 방식)