- 금요일까지 직원 페이지 완료
- 주말부터 수요일까지 1차 개발 완료 목요일 발표 준비 및 테스트


# 📋 직원 페이지 개발 이슈 (Backend & Frontend)

## 🛠 Backend Issues

### 1. 근태 관리 (Attendance Domain)
- [ ] **내 근태 기록 조회 API 구현** (`GET /api/attendance/my`)
    - 월별 조회 기능 (년, 월 파라미터)
    - 일별 상태 (정상, 지각, 결근, 휴가 등) 및 근무 시간 계산 로직
- [ ] **휴가 신청 API 구현** (`POST /api/attendance/leave`)
    - 연차, 반차, 워케이션 등 유형별 신청 처리
    - 잔여 연차 차감 로직 연동
- [ ] **출퇴근 처리 API** (필요 시)
    - 이미 구현되어 있는지 확인 필요, 없다면 `POST /api/attendance/clock-in`, `clock-out` 추가

### 2. 부서/팀원 관리 (Member/Department Domain)
- [ ] **팀원 목록 조회 API 구현** (`GET /api/members/team`)
    - 같은 부서 팀원들의 기본 정보 및 **현재 상태** (업무중, 자리비움, 휴가 등) 조회
    - 실시간 상태 반영을 위한 로직 점검

### 3. 포인트 몰 & 미션 (Gifticon & Mission Domain)
- [ ] **상품 목록 조회 API** (`GET /api/gifticons`)
    - 재고 수량 포함하여 조회
- [ ] **내 포인트 조회 API** (`GET /api/points/my`)
- [ ] **상품 구매/교환 API** (`POST /api/gifticons/purchase`)
    - 포인트 차감, 재고 감소, 구매 이력 저장 트랜잭션 처리
- [ ] **미션 목록 및 현황 조회 API** (`GET /api/missions/my`)
- [ ] **미션 달성/보상 수령 API** (`POST /api/missions/{id}/claim`)

---

## 💻 Frontend Issues

### 1. 근태 페이지 (`Attendance.jsx`)
- [ ] Mock Data 제거 및 API 연동 (`attendanceHistory`, `leaveRequests`)
- [ ] 월 이동(`handlePrevMonth`, `handleNextMonth`) 시 데이터 재조회 로직 구현
- [ ] 휴가 신청 모달 제출 핸들러 구현 및 유효성 검사

### 2. 부서 페이지 (`Department.jsx`)
- [ ] `teamMembers` Mock Data 제거 및 API 호출로 변경
- [ ] 팀원 상태 필터링 로직 확인 (Server-side or Client-side)

### 3. 포인트 몰 페이지 (`PointMall.jsx`)
- [ ] 사용자 보유 포인트 API 연동 및 표시
- [ ] 상품 목록(`ShopContainer`) 및 미션 목록(`MissionContainer`) API 연동
- [ ] 상품 구매 버튼 클릭 시 실구매 API 호출 연결
- [ ] 미션 완료 버튼 클릭 시 보상 수령 API 호출 연결

### 4. 공통
- [ ] API 호출 실패 시 에러 핸들링 (Toast 알림 등)
- [ ] 로딩 상태 UI 처리 (`Suspense` or `LoadingSpinner`)